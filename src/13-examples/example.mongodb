use("platzi_store")

db.iot.updateMany(
    // query
    {sensor: "A001"}, //eq por defecto

    //update
    {
        $pull:{
            readings: //quitamos datos de un array
            //usamos posteriormente un operador de query
            {$gte: 3} //elimina elementos de ese array,
            //mayores o igual que 3
        }
    }
)

